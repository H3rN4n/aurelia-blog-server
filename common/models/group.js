'use strict';
//var User = require('./user.js');

module.exports = function(Group) {

    // Group.embedsOne(User, {
    //     as: 'user', // default to the relation name - address
    //     property: 'users' // default to addressItem
    // });

};
